import 'lodash';
import moment from 'moment';
import 'bootstrap';

//var testModalButton = new bootstrap.Modal(document.getElementById('testModal'))

moment.locale('id');
console.log(moment.locale());